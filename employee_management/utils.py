from management import models as management_models


def get_unread_messages(user):
    # all_unread_message_count = management_models.Message.objects.filter(receiver__users=user).count()
    # print("COunt====", all_unread_message_count)
    count = None
    return count
